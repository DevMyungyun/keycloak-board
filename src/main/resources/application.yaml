
server:
  port: 8005

spring:
  application:
    name: keycloak-example
  main:
    allow-circular-references: true

  datasource:
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://mysql.db.com:3306/board?characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
      username: root
      password: mysql0202!
      # password: root0707!
      pool-name: BoardHikariPool
      connection-test-query: SELECT NOW() FROM dual

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: update
  
  mvc:
    hiddenmethod:
      filter:
        enabled: true 

  devtools:
    livereload:
      enabled: true

  logging:
    level:
      org:
        hibernate:
          SQL: DEBUG
          type:
            descriptor:
              sql:
                BasicBinder: TRACE